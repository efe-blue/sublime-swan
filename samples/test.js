swan.login({
    success: function (res) {

    }
});

swan.getNetworkType();

let ctx = swan.createCameraContext();

ctx.startRecord({
    
});

ctx.takePhoto({
    
});

let ctx1 = swan.createCanvasContext('myCanvas');
ctx1.setLineWidth(10);
ctx1.arc(x, y, r);

